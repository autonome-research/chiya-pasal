# Matcha
matcha/matcha.db
matcha/logs/
matcha/bin/matcha
matcha/collect.lock
matcha/*/arxiv_trickle.lock

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Wiki generated content (optional — uncomment if you don't want to track articles)
# wiki/raw/articles/

# Secrets
.env
*.json
token*
*.key

# OS
.DS_Store
Thumbs.db

# Codex
.codex

# pipelines/ TS project — track config files explicitly, ignore build artifacts
!pipelines/package.json
!pipelines/tsconfig.json
!pipelines/__tests__/**/*.json
!pipelines/src/**/*.json
pipelines/node_modules/
pipelines/dist/
pipelines/package-lock.json
pipelines/*.db
pipelines/*.db-*

# Reference code (deleted after librarian/digest cutover)
orchestrator/
